import type { JsonData } from "./json-data";

export class FancyUIService {
	public displayMenus(jsonData: JsonData): void {
		// Make use of the JsonData to fetch menus
	}

	public displayRecommendations(jsonData: JsonData): void {
		// Make use of the JsonData to load recommendations
	}
}
